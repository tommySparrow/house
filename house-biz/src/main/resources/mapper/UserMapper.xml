<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.house.house.mapper.UserMapper" >

    <!--插入用户-->
  <insert id="insert">
    insert into user (
                name,
                phone,
                email,
                aboutme,
                passwd,
                avatar,
                type,
                create_time,
                enable
                )
                values(
                #{name},
                #{phone},
                #{email},
                #{aboutme},
                #{passwd},
                #{avatar},
                #{type},
                #{createTime},
                #{enable}
                )
   </insert>

    <!--根据用户对象包含的属性,查询对应的用户-->
    <select id="selectUsersByQuery" parameterType="user" resultType="user">
        select * from user
        <where>
            <if test="id !=null" >
                and id = #{id}
            </if>
            <if test="email != null">
                and email = #{email}
            </if>
            <if test="passwd != null">
                and passwd = #{passwd}
            </if>
            <if test="enable != null">
                and enable = #{enable}
            </if>
            <if test = "type != null and type!=0">
                and type = #{type}
            </if>
        </where>
    </select>
</mapper>